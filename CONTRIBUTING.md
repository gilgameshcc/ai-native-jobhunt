# 维护约定

> 给改这个仓库的人看的，包括未来的维护者和提 PR 的人。规则不多，但每一条都是真的踩过才写下来的。

## 1. 仓库里出现的每个编号，仓库里都要有定义

模块编号 `00`-`06`、固件名、契约文件名、章节号（§2.0.1）都可以用，因为读者能在仓库里找到它们指什么。

**不许出现只有维护者自己知道的编号**：测试轮次代号、用户故事编号、决策编号之类。修订记录里写"依据哪次测试"时，写测试的**名称**（区分度测试 / 红线审查 / 独立可用性测试 / 端到端实跑），不写代号。判据很简单：**在仓库里 grep 这个编号，找不到定义就不许用。**

## 2. 不写作者或任何真人的个人事实

- 示例（`examples/`）与固件（`fixtures/`）**全部虚构、从零编**。不摘录真实材料，脱敏也不行——真实材料即使改了名字，仍带着可反查的结构与巧合。虚构公司名统一用：纬度智能 / 恒平科技 / 青栈网络。
- 维护者那次实践的具体数字（面试了几场、投了多少条、跑了多久、账号情况）只在维护者本人明确决定公开时写入，并在同一行标注 `<!-- 作者提供 YYYY-MM-DD -->` 说明来源与日期。没有这个标注的此类数字一律视为不该出现，PR 里不要替维护者补。
- **要写的是局限声明**：这套东西来自一次实践、n=1、阈值未经多人验证、每个数字都标来源等级。局限声明和个人事实的区别：前者说"证据有多强"，后者说"作者经历了什么"。
- 不采集、不记录 HR 或其他候选人的姓名与联系方式（见 README「不做什么」）。

## 3. 提交信息只写改了什么、为什么

提交信息是公开历史的一部分，删不掉。不带会话链接、不带工具 ID、不点名仓库外的文件或目录。决策过程如果值得留，写进对应模块 README 的修订记录，那里能改。

## 4. 版本与契约

- 不动 [`docs/module-contracts.md`](docs/module-contracts.md) 的改动 = patch；动契约 = minor，必须在 [`CHANGELOG.md`](CHANGELOG.md) 写迁移说明；契约文件被删或改名 = major。
- 各模块 README 底部各自记修订；仓库级变更只记契约与迁移。
- 修订记录的标题不用"未发布"这类临时状态词——发布之后没人会回来改它。

## 5. 每条判断规则配一对固件

新增或修改一条会做判断的规则，就在 `fixtures/<模块>/` 加一对 `<规则名>.hit.md` / `<规则名>.miss.md`，并在该目录 `expected.md` 写清判据。两份同时触发或同时不触发 = 这条规则没有区分力，重写或删掉。细节见 [`fixtures/README.md`](fixtures/README.md)。

## 6. 五条边界不接受"用户有需求"的例外

见 README「不做什么」。提 PR 前对照一遍：不指导绕过平台防护、不教操纵初筛、不采集他人信息、不给无上限投递配置、受益人只能是候选人。
