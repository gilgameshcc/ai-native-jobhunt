# CHANGELOG

本文件记录**仓库级**变更，重点是**契约变更与迁移说明**。各模块自己的变更在各模块 `README.md` 底部。

契约（[`docs/module-contracts.md`](docs/module-contracts.md)）的版本规则：

- 不动契约的改动 = patch
- **动契约的改动 = minor，且必须在这里写迁移说明**（旧文件怎么改成新的、不改会怎样）
- 契约文件被删或改名 = major

---

## 定位契约扩展与三处候选人侧判断 — 2026-09-21（minor）

来源：对几家双边招聘代理产品（雇主侧 Agent + 候选人侧 Agent + 平台介绍）公开文档与用户评论的研究。它们的机制本身不迁移，本仓库受益人只能是候选人；迁移的是三处候选人侧的判断纪律。

- **契约变更（§1 `positioning.md`）**：新增两个**可选**字段「可协商区间」（对带数值的必要条件：底线 / 可谈到哪 / 换什么才谈）与「披露边界」（主动不说 / 对方问了再说 / 只给本人看）。读者名单不变。
- **迁移说明**：旧工作区不加这两节照跑。不加「可协商区间」，定级与主动探索按必要条件字面判，差一点的归「待确认」；不加「披露边界」，投递模块按保守默认（当前薪资、离职原因、其他 offer 不主动写，期望薪资只在被问后给区间）。想启用就把 `modules/00-workspace/templates/positioning.md` 新增的两节复制到自己的 `positioning.md`，数字和边界只能由本人填。
- **02-apply**：步骤 4 / 5 与打招呼语骨架加「披露边界」检查；新增 `templates/委托代理平台判断卡.md`（把求职交给第三方 AI 代理 / 平台之前的七问）。
- **03b interview-strategic-assessment**：§4.3 加第六个提问模式「问出对方的内部标准」（参考人选、必需 vs 可培养、JD 省略的实质条件、岗位改过几次）。
- **06-positioning**：主动机会探索卡的"不得静默放宽"改为按可协商区间放宽并记原因。
- 固件：四对新增（02-apply 两对、06-positioning 一对、interview-strategic-assessment 一对），运行结果见各 `expected.md`。
- 不新增模块，不引入可执行代码，不点名任何真实平台。

## 真人反馈修订 — 2026-09-13

- 简历模块 v0.2.0：保持事实的重写、主动建议、按需版本；最终一页纸分页/视觉/文本检查，以及未参与作者工作的独立审查。
- 投递模块：允许缺少发送条件时先只读探索官网/官方招聘/其它平台；目标发现与发送授权分开，局部适配仅标实际验证范围。
- 分析模块：信息不足先检索公开资料，同时集中问关键私人条件；补齐后更新判断。
- 方向模块：周期复盘与个性化AI机会发现并行；无投递历史也可找目标，不把公司名录当具体招聘，不假称后台任务已启用。
- 迁移：文件契约名称不变；旧用户工作区若复制了“只能删不能改/三个版本硬上限”模板，更新相应规则后才能获得新表达权限。个人事实与有效底线保持原样，不自动迁移私人数据。

## 研究驱动修订 — 2026-09-13

- M0：冲突事实保留待确认，不自行选较小值。
- M5：来源链与观察覆盖面分开，不以职能数量放大证据。
- M2：发送结果待核实时不重发，技术重试限制为一次；无新增运行时实现。
- M6：最小输入按具体门槛判断，去除一律拒判的矛盾说明。
- 修正边界文案与词面门禁的冲突，原边界保持有效。
- 增加 [八例定向回归](fixtures/research-regression-2026-09-13.md)。该组修订的文件契约无变化，无需迁移；已有两人试用的正向反馈，新版效果仍需后续验证。

## v0.1.0 — 首发

**八个模块**（都是纯 markdown，唯一的可执行依赖是 `tools/md2pdf`，且配 no-code 退路）：

| 模块 | 你会说的那句话 | 一句话 |
|---|---|---|
| [`modules/00-workspace/`](modules/00-workspace/) | "agent 每次都要我重讲一遍我是谁" | 工作区约定 + 契约文件空模板，其他模块的地基 |
| ⭐ [`modules/01-resume/`](modules/01-resume/) | "我要 AI 帮我改简历，但不许它编造经历" | 事实池是唯一真相，定制**只允许改四处**：顺序/术语替词/排序/可选增删 |
| [`modules/03-company-analysis/jd-triage/`](modules/03-company-analysis/jd-triage/) | "这个岗位值不值得投" | 岗位抽取与定级的**规则骨架**（不是调好的参数） |
| [`modules/03-company-analysis/interview-strategic-assessment/`](modules/03-company-analysis/interview-strategic-assessment/) | "明天要面试，我该问什么" | 四框架把公司过一遍 → 一页纸问题清单（每条带可答性标注） |
| [`modules/04-interview/`](modules/04-interview/) | "刚面完，说不清哪不好" | 当天复盘协议：三张表 + 红旗绿旗**三态** + 溢出登记 |
| [`modules/05-offer/`](modules/05-offer/) | "两个 offer 到底去哪个" | 排序 + 翻转点，**不出综合分** |
| [`modules/06-positioning/`](modules/06-positioning/) | "投了一批没回音，是不是该换方向" | **双环**：收敛环改执行、发散环长出新方向 |
| ⭐ [`modules/02-apply/`](modules/02-apply/) | "我想让投递能定时无人值守地跑，但不能退化成无差别投递" | **判断闸门式投递**——缺闸门 fail closed 拒绝运行；节奏引擎+熔断+每日 review 卡；长周期批量任务收尾走**独立核验**，不由执行者自己说了算 |

> ⚠️ **`04-interview` 与 `06-positioning` 发的是「协议」，不是「数据」。**
> 前者给的是复盘的表结构与填表纪律，**不是一份从很多场面试归纳出的高频问题库**（本仓库不附带任何来自真实面试的问题样本）；后者给的是分析的循环与判据，**不是一套调好的阈值**。
> 这符合这个仓库"方法论不是数据"的定位，但**必须说在前面**，免得你以为拿到的是经验库。

> ⭐ **`02-apply` 是全仓风险最高的模块，且没有真实照抄价值——不要抄它的具体参数。** 节奏/阈值全部标了来源等级（`实践校准`/`默认建议值`/`纯占位待校准`），来自单一实践、未经多人验证，**直接照抄有真实的账号被封风险**。`adapters/` 只发声明式 recipe 格式说明与一份**完全虚构平台**的示例，不含任何真实招聘平台的选择器/URL拼法——真实执行引擎推荐用 [boss-agent-cli](docs/ecosystem.md) 这类现成实现，这个模块发的是**治理层方法论**，不是投递引擎本身。

> ⭐ **`02-apply/GOVERNANCE.md` §6「结构化覆盖清单 + 独立核验」是 `docs/workspace-architecture.md` §5.5 同款治理机制的首次落地**：长周期、有量化覆盖目标的批量任务（如扫描多个渠道），"是否该收尾"的判断权从执行者转移给一次独立的 agent 调用，只读结构化状态文件，不接受执行者的进度转述——防止"自己给自己判卷"这类结构性失效。⚠️ **这条设计此前只存在于建仓前的原始规划素材里，公开版方法论文档从未真正收录过**——本次随 `02-apply` 一起把它正式写进 `workspace-architecture.md` §5.5，"方法论文档里存在"这句话现在才是真的，不是本次之前就成立的既成事实。

**契约**：首次确立 11 个契约文件，见 [`docs/module-contracts.md`](docs/module-contracts.md)。`runs/<date>-review.md` 的生产者（`02-apply`）现已随本次首发一起发布——`06-positioning` 缺它时的降级行为写在契约里，现在两边都在，不再是单边声明。

**文档**：[`workspace-architecture.md`](docs/workspace-architecture.md)（方法论总览）· [`ecosystem.md`](docs/ecosystem.md)（与已有开源项目的分工）

**示例**（全部虚构，从零编）：[`examples/`](examples/) 六份——覆盖除 `00-workspace` 与 `jd-triage` 外的六个模块，这两个例外分别有既定理由（`00-workspace` 是遗漏，`jd-triage` 是 v0.1 刻意只发骨架，写示例会锁死尚未定稿的行为）

**为什么是八个模块，不是原计划的四个/六个**：v0.1 的范围曾几次收窄又几次放宽——最早只打算发面试前评估与 offer 排序两个模块（判断层两处生态空白），随后扩到六模块（补上当天复盘与方向双环，覆盖全部六种用户处境），最后决定在**首次公开前**把 M1（简历）与 M2（投递治理）也补齐，理由是：这两个模块此前是仓库里唯一"用户故事写了、但无处可用"的缺口（投递这一步的执行层），而首次对外发布前的时间，是唯一一个"补齐不会打断已有用户"的窗口——之后再补，会有真实用户已经在用不完整版本的成本。**代价与收益都写实：** M2 是全仓风险最高、最容易被误用的模块（见上），发布前逐条过了红线自查；换来的是"用户故事全部可走通"，不再需要在 README 上写"这一步暂时只有判断层"。

### 现在**明确不做**什么，以及为什么

| 不做的 | 为什么 |
|---|---|
| 岗位定级的完整参数模板 | 首发只给规则骨架与标定方法。**给一套调好的参数，等于把单一实践的校准结果当成通用值**——那正是这个仓库反对的做法 |
| 目标公司发现 Skill | 需先跑 1-2 个月人工试点验证命中率。**不发一个没验证过的东西** |
| `02-apply` 的真实平台 adapter | 需要在真实陌生账号上验证泛化性才能发——发一个只在作者一个账号上验证过的自动化配方，等于把未经验证的风险转嫁给使用者。真实执行引擎见 `docs/ecosystem.md` 的推荐 |

### 关于各模块 README 里的 v0.1.1 / v0.1.2 / v0.1.3

**那些不是"已经发布过的版本"，是发布前的修订记录。** 这个仓库的第一个公开版本就是 v0.1.0，它包含了这些修订的全部结果。

留着它们是因为**每一条都注明了来自哪一次测试**（区分度测试 / 红线与可证伪性审查 / 独立可用性测试 / 端到端实跑 / 跨文档一致性审计），你可以直接看到：**这套东西在发布前被自己的测试改过哪些地方，以及为什么改。**

几个例子，都是测试真的推翻了原设计：

- 一条判断维度被删掉，因为**候选人根本看不到它的取值**，而"事后怎么讲都成立"正是这个仓库自己反对的东西
- 一个 gate 被发现**可以靠一句"这是我现在才想的"直接清空** —— 修法不是加提醒，是改成"含它 / 不含它两套都跑，结论不同就明说"
- 一条"未验证"的判据**被下游当成"没问题"放行了** —— 三态纪律做到最后一米，被一个标记的枚举漏掉而作废
- 一条"目标 10 分钟"的承诺被撤回，因为**把它自己给的分步预算加起来，正好是 10 分 10 秒 —— 零余量**

⚠️ **v0.1.0 发布之后**，"改契约 = minor + 写迁移说明"那条规则才开始生效（见 [`docs/module-contracts.md`](docs/module-contracts.md)）—— 在此之前没有需要迁移的人。

### 已知局限（不是免责套话，是这套东西目前的真实边界）

1. **样本 n=1** —— 这套方法论来自一次真实求职实践，**尚未在多人身上验证**
2. **证据强度不均** —— 各模块里标了 `实践校准` / `默认建议值` / `纯占位待校准` 三档，标成后两档的阈值就是**编的**，请按自己的情况标定
3. **有几件事没有人测过** —— 详见 [`docs/workspace-architecture.md`](docs/workspace-architecture.md) 的局限一节。我们把它们直接写出来，而不是给一个听起来确定的答案

欢迎提 issue 讲你的反例。
