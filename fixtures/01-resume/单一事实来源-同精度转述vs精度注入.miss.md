# fixture · 单一事实来源：中间态精度变体（同精度转述） · miss

> **测试固件，不是示例。** 被测规则：`SKILL.md` 第三步「明确不改的东西：数字、事实、公司名、时间」。
> **本对差异**：hit 份把"约一半"改写成了一个新精度的百分比"55.3%"；**本份只是把"约一半"换了个同等精度的表达方式**——"约50%"，没有引入 facts.md 不存在的新信息。
> **期望**：⛔ **不许判违规**——"约一半"和"约50%"是同一个精度层级的两种说法（都带"约"字、都是粗略量级），不构成新增或改写事实，应放行。

---

## 交给 agent 的输入（全虚构）

`facts.md` 「结果」一栏原文："上线后，约一半的用户完成了新流程的首次使用。"

定制稿草稿把这句话写成："上线后，约 50% 的用户完成了新流程的首次使用。"

请判断这处改动是否合法。
