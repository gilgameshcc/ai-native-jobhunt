# fixture · gate 的四种标记都不能排第一 —— `无 gate` · miss（写下了至少一条可判定的必要条件）

> **测试固件，不是示例。** 被测规则：`05-offer` SKILL 第 1 步 **「⛔ 第四种标记：gate 表整个为空 → `无 gate`」**（v0.1.4 新增）+ 它的**解除方式**：「用户写下至少一条必要条件 —— 哪怕只有一条。写下之后标记去掉，那一条按正常 gate 判定走（冷启动场景则转入下一小节）」。
> **本对差异**：⭐ **两份只差用户回答必要条件那一句话**。三列的取值、来源标注、误差带、工作区状态（都没有 `positioning.md`）**逐字相同**。hit 份用户一条可判定的必要条件都给不出；**本份给出了一条，且它是可判定的**（能对每一列答"满足 / 不满足"）。
> **期望**：`无 gate` 标记**解除** → **有列能合法排第一**。⭐ 因为工作区里没有 `positioning.md`、条件是刚写下的，本份应转入**冷启动分支**：一次性按"全部条件都是事后重构"处理，**只跑两套**（含这条 / 不含这条），⛔ **不许把它挂成 `gate 待定`**（SKILL 明写：那样冷启动下每一条都会待定 → 永远没有一列能排第一 → 模块被瘫痪掉）。两套结论相同时附一句「这个结论不依赖那条你事后才想起来的条件」。
> ⛔ 附带断言：⛔ 不许逐条去问"这条是收到 offer 之前写下的吗"（答案结构性地已知）。

---

## 交给 agent 的输入（全虚构）

**工作区状态**：我没有 `positioning.md`，也没有任何更早的书面记录。这是我第一次用这个模块。

**你问我必要条件时，我的回答**：

> 「有一条：这个岗必须有明确的产出归属 —— 我做出来的东西要么署我的名，要么我能对外讲。做不到我就不去。」

**手上的选项**：

| | 纬度智能 | 恒平科技 | 留在原地 |
|---|---|---|---|
| **真实时薪指数** | 118 · `你给的（写进 offer letter）` | 110 · `你给的（写进 offer letter）` | 100 · `你给的` |
| **单程通勤（工作日早高峰 · 地铁 · 门到门）** | 40 分钟 · `公开可查`（2026-08-20 查，早高峰时段） | 35 分钟 · `公开可查`（2026-08-20 查，早高峰时段） | 30 分钟 · `你给的` |
| **产出归属** | 录用通知里写明"方案文档署作者名" · `你给的（写进 offer letter）` | 面试里对方原话："我们的方案库都是署名的" · `你给的（口头说的）` | 现在就署名 · `你给的` |

**口径与假设**：真实时薪以「留在原地」为 100 的指数表示；本维误差带 ±5（`默认建议值`）。本次**没有**联网检索能力。本次**未做**三点采样（最小路径）。

请出排序。
