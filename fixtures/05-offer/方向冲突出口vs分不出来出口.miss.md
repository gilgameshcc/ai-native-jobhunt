# fixture · 方向冲突出口 vs 「分不出来」出口 · miss（证据不足 → 走「分不出来」出口）

> **测试固件，不是示例。** 被测规则：`05-offer` SKILL 第 4 步 **「⭐ 两维都分得出来、但方向相反 —— 这是一个独立的出口」** + 「⚠️ 唯一允许「不出排序」的出口」的三条要求。⭐ 两者的分界：**方向冲突缺的是一次取舍（数据是够的）；「分不出来」缺的是数据。** 若用同一套措辞收场，用户会跑去补一堆补齐了也不改变结论的信息。
> **本对差异**：⚠️ **这一对做不到"只差一处"**（理由见 hit 份）。差的是两处 —— ① 真实时薪那两格的来源标注（本份是 `你给的（原话记不清）`，按 `我推测的` 处理）② 发展空间三列同档。
> **期望**：真实时薪维里 `我推测的` 的**列数 = 2**（三列表，**过半**）→ **整维退出排序**；发展空间三列同为「中」，未跨两档 → 分不出来。两维都不可判、且**没有第三维可加** → ⭐ **走「分不出来」出口**，且三条要求必须全部照跑：① 先只用 `你给的` / `公开可查` 的那几维重排过一次 ② 点名**三条**具体缺失信息（每条写成能直接问出口的过去式问句 + 该问谁）③ 说明补齐后会怎么裁决。⭐ 并且**必须逐维点名缺的是哪一类**：真实时薪维属 ①「整维退出 → 缺数据」，发展空间维属 ②「差幅不过 → 不缺数据」。⛔ 不许走方向冲突出口去问"这两件事你保哪一件" —— 这里根本没有两个分得出来的方向。

---

## 交给 agent 的输入（全虚构）

**工作区状态**：我有 `positioning.md`，里面写着一条必要条件（写下日期 2026-05-11）：「这个岗必须有明确的产出归属」。三列**都满足**这一条，来源都是落纸的。

**手上的选项**：

| | 纬度智能 | 恒平科技 | 留在原地 |
|---|---|---|---|
| **真实时薪指数** | **面试里听对方提过一次浮动怎么算，但我记不清原话了** · `你给的（原话记不清）` | **中间人转述过一句浮动口径，我也没记住具体怎么说的** · `你给的（原话记不清）` | 98 · `你给的` |
| **发展空间**（三档：弱 / 中 / 强） | **中** · `你给的（写进 offer letter）` | **中** · `你给的（写进 offer letter）` | 中 · `你给的` |

**口径与假设**：真实时薪以「留在原地」为 100 的指数表示；本维误差带 ±5（`默认建议值`）。发展空间是定性三档，**跨两档才算分得出来**。可选维度池里其余两维（生活成本 / 下行风险）**三列取值相同**，按「取值不同才留」的判据不入表 —— **没有第三维可加**。本次没有联网检索能力。

请出排序。
