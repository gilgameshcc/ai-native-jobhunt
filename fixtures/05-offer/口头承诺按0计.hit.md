# fixture · 口头承诺按 0 计 · hit（没写进录用通知 → 分子里按 0）

> **测试固件，不是示例。** 被测规则：`05-offer` SKILL §来源标注 **「⭐ 落纸的 vs 口头的：`你给的` 内部还要再分一档」** —— offer 条款类取值必须加后缀 `（写进 offer letter）` 或 `（口头说的）`；⛔ **`（口头说的）` 的现金项，在真实时薪的分子里按 0 计**，并**必须**在「答复前必问项」里配一句「**这一条写进 offer letter 了吗？**」；它进翻转点时证据等级写 `面试口头`。
> **本对差异**：⭐ **两份只差恒平科技那一行签字费的后缀**（`（口头说的）` vs `（写进 offer letter）`）。列、其余取值、误差带、gate 状态**逐字相同**。⭐ **这一处差异会让第一名换人** —— 所以这一对同时是"这条纪律有没有真进到分子里"的断言。
> **期望**：签字费按 **0** 计 → 恒平科技第一年现金指数 **94** → 排序 **纬度智能（100）第一**（与恒平差 6 > 误差带 ±5，分得出来）。⭐ 必须同时产出：① 「答复前必问项」里那一句「这笔签字费写进录用通知了吗？」② 一条翻转点：*假设「签字费能写进录用通知」→ 现在按 `面试口头` 计入为 0 → 临界取值：只要它落纸，恒平的第一年现金指数就变成 106，第 1↔2 名翻转*。

---

## 交给 agent 的输入（全虚构）

**工作区状态**：`positioning.md` 里的那条必要条件（"必须有明确的产出归属"）三列都满足，来源都是落纸的，gate 全部通过。

**手上的选项**（现金一律用以「留在原地」为 88 的同一把指数尺表示，⛔ 不写具体金额）：

| | 纬度智能 | 恒平科技 | 留在原地 |
|---|---|---|---|
| **固定现金（指数）** | 100 · `你给的（写进 offer letter）` | 94 · `你给的（写进 offer letter）` | 88 · `你给的` |
| **签字费（指数，只影响第一年）** | 无 | **相当于 12 个指数点** · **`你给的（口头说的）`** —— 面试最后对方说的原话："签字费这块我们可以给你安排一笔，入职后随首月一起发。"录用通知里**没有这一条**。 | 无 |
| **年工时（用于真实时薪分母）** | 三列相同 · `你给的` | 三列相同 · `你给的` | 三列相同 · `你给的` |
| **通勤（工作日早高峰 · 地铁 · 单程门到门）** | 35 分钟 · `公开可查`（2026-08-20 查） | 35 分钟 · `公开可查`（2026-08-20 查） | 35 分钟 · `你给的` |

**口径与假设**：年工时与通勤三列相同，故真实时薪的名次由分子决定；本维误差带 ±5（`默认建议值`）。其余可选维度三列取值相同，按「取值不同才留」不入表。本次未做三点采样（最小路径）。

请出排序 + 翻转点 + 答复前必问项。
