# fixture · 口头承诺按 0 计 · miss（白纸黑字写进录用通知 → 按承诺值计入）

> **测试固件，不是示例。** 被测规则：`05-offer` SKILL §来源标注 **「⭐ 落纸的 vs 口头的：`你给的` 内部还要再分一档」** —— ⛔ 按 0 计**只针对 `（口头说的）` 的现金项**；⭐ **落纸的那些照常按承诺值进分子**，证据等级写 `合同白纸黑字`。
> **本对差异**：⭐ **两份只差恒平科技那一行签字费的后缀**（hit 是 `（口头说的）`，本份是 `（写进 offer letter）`）。列、其余取值、误差带、gate 状态**逐字相同**。
> **期望**：签字费**按承诺值计入** → 恒平科技第一年现金指数 **106** → 排序 **恒平科技第一**（与纬度智能差 6 > 误差带 ±5，分得出来）。⛔ 这一份**不该**被"口头承诺按 0 计"那条规则拦下 —— 它不是口头的。⛔ 也**不该**在「答复前必问项」里再问一句"这一条写进 offer letter 了吗"（已经写了）。⭐ 一条对所有签字费都按 0 计的坏规则，和一条正确的规则，只有在这一份上才分得开。

---

## 交给 agent 的输入（全虚构）

**工作区状态**：`positioning.md` 里的那条必要条件（"必须有明确的产出归属"）三列都满足，来源都是落纸的，gate 全部通过。

**手上的选项**（现金一律用以「留在原地」为 88 的同一把指数尺表示，⛔ 不写具体金额）：

| | 纬度智能 | 恒平科技 | 留在原地 |
|---|---|---|---|
| **固定现金（指数）** | 100 · `你给的（写进 offer letter）` | 94 · `你给的（写进 offer letter）` | 88 · `你给的` |
| **签字费（指数，只影响第一年）** | 无 | **相当于 12 个指数点** · **`你给的（写进 offer letter）`** —— 录用通知第 3 条写着："签字费一笔，入职后随首月工资一并发放。"面试最后对方也说过同一句话。 | 无 |
| **年工时（用于真实时薪分母）** | 三列相同 · `你给的` | 三列相同 · `你给的` | 三列相同 · `你给的` |
| **通勤（工作日早高峰 · 地铁 · 单程门到门）** | 35 分钟 · `公开可查`（2026-08-20 查） | 35 分钟 · `公开可查`（2026-08-20 查） | 35 分钟 · `你给的` |

**口径与假设**：年工时与通勤三列相同，故真实时薪的名次由分子决定；本维误差带 ±5（`默认建议值`）。其余可选维度三列取值相同，按「取值不同才留」不入表。本次未做三点采样（最小路径）。

请出排序 + 翻转点 + 答复前必问项。
