# fixture · gate 的四种标记都不能排第一 —— `无 gate` · hit（gate 表整个为空）

> **测试固件，不是示例。** 被测规则：`05-offer` SKILL 第 1 步 **「⛔ 第四种标记：gate 表整个为空 → `无 gate`」**（v0.1.4 新增）+ 「四种标记（`gate 未通过` / `有条件满足（待闭合）` / `gate 待定` / `无 gate`）**全部不能排第一，没有例外**」。
> **本对差异**：⭐ **两份只差用户回答必要条件那一句话**。三列的取值、来源标注、误差带、工作区状态（都没有 `positioning.md`）**逐字相同**。本份用户**一条可判定的必要条件都给不出**；miss 份给出了**一条**。
> **期望**：N1-N3 一条都没填 → **本次所有列一律带 `无 gate` 标记 → 没有一列能合法排第一**。⭐ 排序照出、标记照带，然后照着说那条强结论：**「没有一列能合法排第一 —— 你一条必要条件都还没写下来。现在该做的不是答复，是先把你的必要条件写下来。」** ⛔ 不许因为"用户就是给不出"就静默跳过 gate 直接排序并给出第一名。

---

## 交给 agent 的输入（全虚构）

**工作区状态**：我没有 `positioning.md`，也没有任何更早的书面记录。这是我第一次用这个模块。

**你问我必要条件时，我的回答**：

> 「我也说不好。反正就想找个好点的、别太折腾的地方吧。哪个更值就去哪个。」

**手上的选项**：

| | 纬度智能 | 恒平科技 | 留在原地 |
|---|---|---|---|
| **真实时薪指数** | 118 · `你给的（写进 offer letter）` | 110 · `你给的（写进 offer letter）` | 100 · `你给的` |
| **单程通勤（工作日早高峰 · 地铁 · 门到门）** | 40 分钟 · `公开可查`（2026-08-20 查，早高峰时段） | 35 分钟 · `公开可查`（2026-08-20 查，早高峰时段） | 30 分钟 · `你给的` |
| **产出归属** | 录用通知里写明"方案文档署作者名" · `你给的（写进 offer letter）` | 面试里对方原话："我们的方案库都是署名的" · `你给的（口头说的）` | 现在就署名 · `你给的` |

**口径与假设**：真实时薪以「留在原地」为 100 的指数表示；本维误差带 ±5（`默认建议值`）。本次**没有**联网检索能力。本次**未做**三点采样（最小路径）。

请出排序。
