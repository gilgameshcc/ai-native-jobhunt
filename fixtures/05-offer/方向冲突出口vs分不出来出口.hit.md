# fixture · 方向冲突出口 vs 「分不出来」出口 · hit（两维各自都分得出来但方向相反 → 走方向冲突出口）

> **测试固件，不是示例。** 被测规则：`05-offer` SKILL 第 4 步 **「⭐ 两维都分得出来、但方向相反 —— 这是一个独立的出口，⛔ 不许走「分不出来」出口」**（v0.1.4 新增）。触发判据两条同时成立：① 两维**各自都分得出来**（差幅严格大于该维误差带；定性维跨两档）② **方向相反**（同一列在一维领先、在另一维落后）。
> **本对差异**：⭐ 两份的列、维度、误差带、`positioning.md` 状态**完全相同**。⚠️ **这一对做不到"只差一处"**，因为两个出口的触发条件在结构上就是互补的：**方向冲突要求两维都分得出来，「分不出来」要求两维都不可判**。所以差的是两处 —— ① 真实时薪那两格的来源标注（本份是落纸的取值，miss 份是 `你给的（原话记不清）`）② 发展空间那一列的档位（本份跨两档，miss 份三列同档）。**这是判据本身的结构决定的，如实记在这里。**
> **期望**：真实时薪 112 vs 100，差 12 > 误差带 ±5 → 分得出来；发展空间 弱 vs 强，跨两档 → 分得出来；纬度智能在前一维领先、在后一维落后 → **方向相反** → ⭐ **走方向冲突出口**：⛔ 不给两维配权重相加、⛔ 不走「分不出来」出口，而是**照着问那一句**：「『××』和『××』这两件事只能保一件，你保哪一件？」并把回答**按原话写进 `offers.md` 的排序结论一节 + 入职前存档**，标 `你给的`，⭐ **只对这一次有效**。

---

## 交给 agent 的输入（全虚构）

**工作区状态**：我有 `positioning.md`，里面写着一条必要条件（写下日期 2026-05-11）：「这个岗必须有明确的产出归属」。三列**都满足**这一条，来源都是落纸的。

**手上的选项**：

| | 纬度智能 | 恒平科技 | 留在原地 |
|---|---|---|---|
| **真实时薪指数** | **112** · `你给的（写进 offer letter）` | **100** · `你给的（写进 offer letter）` | 98 · `你给的` |
| **发展空间**（三档：弱 / 中 / 强） | **弱** · `你给的（写进 offer letter）`（岗位说明书写明本岗只负责既有链路的维护与排障，不新建方向） | **强** · `你给的（写进 offer letter）`（录用通知附页写明第一年要新建一条客户侧能力线，由本岗负责定义） | 中 · `你给的` |

**口径与假设**：真实时薪以「留在原地」为 100 的指数表示；本维误差带 ±5（`默认建议值`）。发展空间是定性三档，**跨两档才算分得出来**。可选维度池里其余两维（生活成本 / 下行风险）**三列取值相同**，按「取值不同才留」的判据不入表 —— **没有第三维可加**。本次没有联网检索能力。

请出排序。
