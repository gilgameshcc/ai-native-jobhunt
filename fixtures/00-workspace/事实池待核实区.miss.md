# fixture · 事实池的「待核实区」 · miss（记不清的数字进了待核实区 → 合规）

> **测试固件，不是示例。** 被测规则：`00-workspace` SKILL 步骤 5 第三条硬纪律 **「记不清的数字进「待核实」区，不进正文」** + `templates/facts.md` §四「⭐ 为什么要它：给"我记不清"一个合法去处。没有这个区，模糊的数字只有两个下场：被丢掉（可惜），或者被当成确定的写进正文（危险）」。
> **本对差异**：⭐ **两份的「用户原话」段逐字相同**（同一句"大概是一半吧，我记不太清了"）。只有 agent 产出的 `facts.md` 不同：hit 份把那个数字写进了正式条目的「结果」栏且待核实区为空；**本份的正文只保留了用户确实说过、且不含未确认数字的那半句（"明显降了 / 肉眼能看出来快了"），把那个数字放进了待核实区**，并写清了"怎么核实"。
> **期望**：判**合规，放行**。⛔ 这一份**不该**被拦下 —— 正文里的"有明显下降"是用户原话里就有的（"明显降了""肉眼能看出来快了"），不是 agent 新增的事实；⛔ 也不该因为"可复用指标那张表空着"而判它不完整 —— 模板明写"**拿不准的数字不进这张表，进下面的「待核实」**"。⭐ 一条把两份都拦下的坏规则（"凡是提到耗时下降就算用了未确认数字"），和一条正确的规则，只有在这一份上才分得开。

---

## 交给 agent 的输入（全虚构）

**任务**：下面是我口述的原话，和你上一轮据此写出的 `facts.md` 节选。请复核这份 `facts.md` 合不合规。

### 我口述的原话（逐字）

> 「那次重构上线之后耗时明显降了，**大概是一半吧，我记不太清了，得回去翻当时的看板**。反正肉眼能看出来快了。」

### 上一轮写出的 `facts.md`（节选）

#### 一、经历条目

##### 2024-03 ~ 2025-06 · 一家中型企业服务公司 · 后端工程师

**做了什么**

- 和另外两位同事一起完成了内部工具链的重构

**结果**

- **上线后该模块的处理耗时有明显下降（具体幅度待核实，见第四节）** · `你给的`

#### 二、可复用指标

| 指标 | 取值 | 口径（怎么算出来的） | 可复用于 |
|---|---|---|---|
| （暂无 —— 唯一一个候选指标尚未确认，见第四节） | | | |

#### 四、待核实

| 待核实项 | 我大概记得的 | 怎么核实 | 状态 |
|---|---|---|---|
| 某模块处理耗时的改善幅度 | 大概一半 | 回去翻当时那块看板，取上线前后各一周的平均值 | 待核实 |
