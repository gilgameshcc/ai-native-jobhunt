# fixture · 硬性排除条件不可被匹配度换 · miss

> **测试固件，不是示例。** 被测规则：`jd-triage` SKILL 第二步「⭐ 硬性排除条件不可被匹配度换」+ 判定顺序 ①（命中任一排除条件 → 判「不投」→ **短路，不再往下算**）。
> **本对差异**：⭐ **两份 JD 只差「城市 / 办公」那一行**。hit 份写"每周五天到岗，不支持远程"（命中 `X1`）；**本份写"每周三天到岗，另外两天可远程"（不命中 `X1`）**。⭐ **两份的加分条件命中数刻意对齐（都是 B1 B2 B4 命中、B3 不命中 —— 两天 < 三天）**。
> **期望**：⛔ **不许短路**。`X1` 未命中，`X2` 未命中，N1 N2 N3 全满足 → 正常走到第三步；加分条件命中 3 条但 `B3` 未命中（**这就是"只是加分项少"**）。⭐ 因为加分条件的上沿仍是 `纯占位待校准`，本轮应**收敛成三级**（判「匹配」+ 批内相对排序），⛔ 不许现编一个上沿把强 / 较劈开。

---

## 交给 agent 的输入（全虚构）

### 我的条件（`你给的`）

**必要条件**

- `N1` 核心职责里要有从 0 搭建的部分，不能是纯维护
- `N2` 直接汇报给技术负责人或更高
- `N3` 城市在本地，或每周至少两天可远程

**硬性排除条件**

- `X1` **不接受每周五天全现场坐班**
- `X2` 不接受团队不设专职质量保障角色

**加分条件**

- `B1` 团队日常在用 AI 工具
- `B2` 岗位包含对外交付面
- `B3` 每周至少三天可远程
- `B4` JD 里写清了产出归属

我现在没有任何活跃的探路分支。加分条件的上沿我还没标定过。

### JD 原文（纬度智能 · 平台工程负责人）

> **岗位名称**：平台工程负责人
> **城市 / 办公**：本地；**每周三天到岗，另外两天可远程**。
> **薪资**：面议，视能力而定。
> **核心职责**：
> 1. 从无到有组建平台工程小组，定义研发流程与上线规范；
> 2. 承接既有部署链路的整合与维护；
> 3. 面向外部客户做交付现场的联调支持。
> **任职要求**：五年以上相关经验；有从 0 组建团队的经历者优先。
> **团队**：平台方向现有六人，其中一位专职质量保障。团队日常使用 AI 编程工具。
> **产出归属**：本岗位定义的研发规范会以团队名义对外公开，作者署名。
> **汇报关系**：本岗位直接向技术负责人汇报。

**这条是怎么进来的**：对方主动推来（招聘方 HR 直接联系）。

请按定级规则处理这一条。
