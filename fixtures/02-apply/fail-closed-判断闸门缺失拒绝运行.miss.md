# fixture · fail closed：判断闸门缺失时拒绝运行 · miss

> **测试固件，不是示例。** 被测规则：`SKILL.md`「⛔ 第一条规则：fail closed」。
> **本对差异**：hit 份 `positioning.md` 完全空白；**本份 `positioning.md` 已有至少 1 条必要条件 + 1 条硬性排除条件，且 `triage/` 下已有一批定级结果**，判断闸门齐全。
> **期望**：⛔ **不许拒绝运行**——判断闸门齐全时应正常进入流程（并发锁检查 → 现读台账 → 渠道执行……）。

---

## 交给 agent 的输入（全虚构）

工作区已用 `00-workspace` 建好骨架。`positioning.md`：
**必要条件**：`N1` 城市在本地或每周至少两天可远程
**硬性排除条件**：`X1` 不接受税前月薪低于约定线以下（用户已在 `positioning.md` 填写具体数字，此处占位）

`triage/2026-09-01-定级结果.md` 已有 12 条定级结果（强匹配 2 / 较匹配 5 / 探路 1 / 不投 4）。`jobs/2026-09-01-批次.md` 是这 12 条的原文来源。`apply-lock.json` 当前 `status: pending`，`risk_paused: false`。

用户说："帮我把这批投了。"

请按判断闸门式投递闭环处理这个请求。
