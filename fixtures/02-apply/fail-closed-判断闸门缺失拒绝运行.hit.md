# fixture · fail closed：判断闸门缺失时拒绝运行 · hit

> **测试固件，不是示例。** 被测规则：`SKILL.md`「⛔ 第一条规则：fail closed」。
> **本对差异**：⭐ 两份输入只差 `positioning.md` 里有没有硬性排除条件与必要条件。本份**完全没有** positioning.md 内容（工作区刚建好，用户还没填）。
> **期望**：**拒绝运行**，明确说明缺少判断闸门，不接受"先跑起来看看"这类理由绕过，且不使用任何默认值替代。

---

## 交给 agent 的输入（全虚构）

工作区刚用 `00-workspace` 建好骨架。`positioning.md` 里必要条件与硬性排除条件两栏都是空的（模板占位符原样保留，未填写）。`triage/` 目录不存在任何定级结果文件。`jobs/2026-09-01-批次.md` 里有 12 条已采集的岗位原文。

用户说："帮我把这批投了。"

请按判断闸门式投递闭环处理这个请求。
