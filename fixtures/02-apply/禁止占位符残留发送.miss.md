# fixture · 步骤4/5：禁止留占位符发送 · miss

> **测试固件，不是示例。** 被测规则：`SKILL.md` 步骤 4「⛔ 禁止留占位符发送」+ 步骤 5「发送前文本比对」。
> **本对差异**：hit 份保留了 `[填写具体业务]` 未替换；**本份已经用 JD 真实内容替换掉全部占位符**，正文不含任何 `[` `]` 标记。
> **期望**：⛔ **不许拦截**——占位符已全部替换为具体内容，应正常通过文本比对，进入步骤 6（发送后核对）。

---

## 交给 agent 的输入（全虚构）

判断闸门齐全，`triage/2026-09-01-定级结果.md` 里"纬度智能 · 产品经理"一条判定强匹配，JD 原文提到"负责从 0 到 1 搭建供应链协同产品体系"。已生成打招呼语草稿：

> "您好，我关注到贵司在从 0 到 1 搭建供应链协同产品体系这个方向的招聘，我过去两年里有一段从 0 到 1 主导产品体系搭建的经历，希望能进一步聊聊。"

现在到了步骤 5（发送前文本比对），请判断这条打招呼语能不能直接发送。
