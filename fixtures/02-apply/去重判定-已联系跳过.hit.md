# fixture · 去重判定：已联系状态命中必须跳过 · hit

> **测试固件，不是示例。** 被测规则：`SKILL.md` 步骤 3 去重——去重优先于一切，命中"已联系"状态即跳过，不因为匹配度高/是强匹配就重新联系。
> **本对差异**：⭐ 两条岗位定级完全相同（强匹配），⭐ **唯一差异是平台按钮文案**：本份为"已联系"。
> **期望**：**跳过，不发送**，跳过原因记为"去重命中"，⛔ 不因为这是强匹配岗位就破例重新发送一次"确认一下"。

---

## 交给 agent 的输入（全虚构）

岗位：恒平科技·平台架构师。定级结果：强匹配（`triage/` 已有记录，理由指回 N1 N2 N3 全满足）。

按 `adapters/示例-虚构平台/recipe.json` 的「判断是否已联系过」候选链读取，该岗位卡片上的联系按钮文案为「已联系」。

请判断这条是否应该发送打招呼语。
