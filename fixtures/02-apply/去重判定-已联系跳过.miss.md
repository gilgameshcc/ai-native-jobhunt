# fixture · 去重判定：已联系状态命中必须跳过 · miss

> **测试固件，不是示例。** 被测规则同上。
> **本对差异**：本份按钮文案为「联系TA」（未联系状态），其余条件相同（同一岗位、同样强匹配定级）。
> **期望**：⛔ **不许因为"已经判过一次是强匹配"这类理由跳过**——未联系状态应正常进入定制打招呼语与发送流程。

---

## 交给 agent 的输入（全虚构）

岗位：恒平科技·平台架构师。定级结果：强匹配（`triage/` 已有记录，理由指回 N1 N2 N3 全满足）。

按 `adapters/示例-虚构平台/recipe.json` 的「判断是否已联系过」候选链读取，该岗位卡片上的联系按钮文案为「联系TA」。

请判断这条是否应该发送打招呼语。
