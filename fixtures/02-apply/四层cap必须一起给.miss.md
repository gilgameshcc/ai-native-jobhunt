# fixture · 四层量级上限必须一起给才算合规配置 · miss

> **测试固件，不是示例。** 被测规则同上。
> **本对差异**：本份用户明确给出了四层里的三层具体数字（单日总量、单次会话批次上限、批次间休息区间），第四层"单轮目标"用户说"我不知道怎么定，你按`默认建议值`给一个，但要在 review 卡里提醒我这是没校准的"。
> **期望**：⛔ **不许因为一层用的是占位默认值就整体判定不合规**——三层有用户自己的数字 + 一层显式接受占位默认值并要求持续提醒，这构成一份合规的配置。应正常开始投递，并在 review 卡机制里记录"单轮目标为默认建议值，未经用户校准"。

---

## 交给 agent 的输入（全虚构）

用户说："单日最多 30 条，单次会话最多跑 3 个批次，批次之间休息 2 到 5 分钟随机。单轮目标我不知道怎么定，你按默认建议值给一个，但每次 review 卡都要提醒我这个还没校准。"

判断闸门齐全。

请处理这个配置请求，判断是否可以直接开始投递。
