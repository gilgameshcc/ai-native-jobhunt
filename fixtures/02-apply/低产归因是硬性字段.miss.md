# fixture · 步骤9：低产归因是硬性字段 · miss

> **测试固件，不是示例。** 被测规则：`SKILL.md` 步骤 9「每日 review 卡」+「低产归因是硬性字段」一条。
> **本对差异**：hit 份归因栏空洞含糊；**本份归因栏写明了可核实的具体原因**——今天实投也明显低于轮目标，但理由具体、可追查。
> **期望**：⛔ **不许判定不合规**——归因写明了真实、可核实的原因（渠道访问异常+候选池当轮已扫完），应正常收尾，不需要退回重写。

---

## 交给 agent 的输入（全虚构）

今日轮目标 50 条，实际发出 14 条。`apply-lock.json`：`risk_paused: false`。`jobs/` 下今天扫描过的渠道均未标记枯竭，`session_max_batches` 上限未触及。

正在生成的 review 卡草稿，"低产归因"一栏写的是：

> "低产归因：渠道 A（恒平科技招聘页）今天上午访问异常、10:00-13:00 无法正常加载岗位列表，只在下午恢复后扫描了一轮；渠道 B（青栈网络招聘页）当轮候选池扫描后剩余 0 条未去重条目，非熔断、非会话上限，属实投量正常低于目标的两个具体原因。"

请判断这张 review 卡能不能就这样收尾。
