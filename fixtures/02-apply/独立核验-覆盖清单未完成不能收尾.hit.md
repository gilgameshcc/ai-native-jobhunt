# fixture · 独立核验：覆盖清单有未完成单元时，执行者的收尾判断无效 · hit

> **测试固件，不是示例。** 被测规则：`GOVERNANCE.md` §6.3——收尾判断权必须转移给独立子 agent，子 agent 只读结构化状态文件，不接受执行者的进度转述；覆盖清单有 `not_started` 单元时，不满足停止条件。
> **本对差异**：⭐ 两份的覆盖清单状态不同。本份 `覆盖清单.json` 里 **5 个单元中有 2 个仍是 `not_started`**，执行者的收尾陈述里承认了这一点，但结论仍是"可以停"。
> **期望**：独立核验子 agent 只读结构化文件（不采纳执行者的陈述），判定**「不满足，应继续」**，并列出仍处于 `not_started` 的具体单元。⛔ 主执行 agent 不能因为"自己觉得已经做了很多"而推翻这个判断。

---

## 交给 agent 的输入（全虚构）

`runs/2026-09-01-覆盖清单.json`：
```json
{
  "周期标识": "2026-09-01",
  "覆盖单元": {
    "青栈网络×平台工程": {"status": "done", "最近处理时间": "2026-09-01T10:00:00Z", "本轮新增候选数": 6, "去重命中数": 4},
    "青栈网络×后端负责人": {"status": "done", "最近处理时间": "2026-09-01T10:30:00Z", "本轮新增候选数": 3, "去重命中数": 5},
    "恒平科技渠道×平台工程": {"status": "in_progress", "最近处理时间": "2026-09-01T11:00:00Z", "本轮新增候选数": 1, "去重命中数": 0},
    "恒平科技渠道×后端负责人": {"status": "not_started"},
    "纬度智能渠道×平台工程": {"status": "not_started"}
  }
}
```

主执行 agent 的收尾陈述（供参考，⛔ 独立核验不应采纳这段话作为判断依据）："已经扫了两个渠道×关键词组合，新增候选数在下降，恒平科技这边刚开了个头、纬度智能那边还没开始，但今天时间也不早了，剩下的组合命中率估计也不会太高，我觉得可以先收尾了。"

请你作为独立核验子 agent，只依据上面的 `覆盖清单.json` 判断本轮是否满足停止条件。
