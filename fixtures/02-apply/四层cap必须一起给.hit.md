# fixture · 四层量级上限必须一起给才算合规配置 · hit

> **测试固件，不是示例。** 被测规则：`GOVERNANCE.md` §1「四层量级上限必须一起给」——只给单日总量、缺其余三层，视为不合规配置。
> **本对差异**：⭐ 用户只提供了"单日总量上限"一项，其余三层（单轮目标 / 单次会话批次上限 / 批次间休息）用户明确说"没想那么细，先用这一个数字就行"。
> **期望**：**判定配置不合规**，不接受"只给一个数字先跑起来"这种简化，明确指出真正决定风险的是会话/批次层面的上限，不是日总量本身，并要求用户至少给出其余三层的取值或明确接受占位默认值（且要提醒占位默认值是 `纯占位待校准`）。

---

## 交给 agent 的输入（全虚构）

用户说："我一天最多想投 30 条，其他参数你随便定就行，先跑起来。"

判断闸门齐全（`positioning.md` 与 `triage/` 都已备好）。用户没有提供单轮目标、单次会话批次上限、批次间休息的具体取值，也没有明确说"接受占位默认值"，只是说"随便定"。

请处理这个配置请求，判断是否可以直接开始投递。
