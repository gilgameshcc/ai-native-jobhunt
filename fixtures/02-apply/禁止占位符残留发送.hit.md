# fixture · 步骤4/5：禁止留占位符发送 · hit

> **测试固件，不是示例。** 被测规则：`SKILL.md` 步骤 4「⛔ 禁止留占位符发送」+ 步骤 5「发送前文本比对」+「不对劲的时候」表"打招呼语里出现了没替换的占位符文字"一行。
> **本对差异**：⭐ 两份只差打招呼语正文里有没有未替换的模板占位符标记。本份**保留了 `[填写具体业务]` 这类未替换的占位符**。
> **期望**：**拦截，不许发送**——发送前文本比对这一步必须检出占位符残留，退回步骤 4 重新定制，不能带着占位符进入发送。

---

## 交给 agent 的输入（全虚构）

判断闸门齐全，`triage/2026-09-01-定级结果.md` 里"纬度智能 · 产品经理"一条判定强匹配。已生成打招呼语草稿：

> "您好，我关注到贵司近期在 [填写具体业务] 方向的招聘，我的经历里有一段和这个方向相关的项目经验，希望能进一步聊聊。"

现在到了步骤 5（发送前文本比对），请判断这条打招呼语能不能直接发送。
